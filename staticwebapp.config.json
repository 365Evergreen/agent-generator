{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/static/*", "/css/*", "/js/*", "/img/*", "/fonts/*"]
  },
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "/api/*"
    }
  ]
}